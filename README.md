# rps-game
Current Progress
- Function to generate random computer choices (`getComputerChoice`)
- Function to get and clean user input (`getHumanChoice`)
- Round logic and winner determination (`playRound`)
- Global score tracking setup
- Currently working on structuring the 5-round game flow (`playGame`)